apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secret
  namespace: default
type: Opaque
stringData:
  APP_KEY: ""       # Set via CI: php artisan key:generate --show
  JWT_SECRET: ""    # Set via CI: GitHub secret JWT_SECRET
  DB_PASSWORD: ""   # Set via CI: GitHub secret DB_PASSWORD
